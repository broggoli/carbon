field f: Int 

// method takePerm(x: Ref)
// requires acc(x.f, sWildcard)
// {}

// function dummy(): Perm 
// ensures result > none

method test(x: Ref)
requires acc(x.f, wildcard)
ensures acc(x.f, wildcard)
{
    //x.f := 2
    var c: Int := x.f
    //inhale acc(x.f, sWildcard)
    // takePerm(x)
    // var c: Int := x.f
    // x.f := 0
}