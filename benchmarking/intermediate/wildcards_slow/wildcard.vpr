field f: Int

method main(x: Ref, y: Ref, z: Ref)
{    
    inhale acc(x.f, wildcard)

    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)

    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)

    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)


    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)


    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)



    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)
    inhale acc(x.f, wildcard)
    inhale acc(y.f, wildcard)
    inhale acc(z.f, wildcard)

    exhale acc(x.f, wildcard)
    exhale acc(y.f, wildcard)
    exhale acc(z.f, wildcard)


    var v: Int := x.f

}